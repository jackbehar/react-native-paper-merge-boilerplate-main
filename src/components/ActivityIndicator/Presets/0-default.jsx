import React from "react";
import ActivityIndicator from "../ActivityIndicator";

export default (
  <ActivityIndicator
    uxpId="ActivityIndicator-1"
    animating={true}
    size="small"
    hidesWhenStopped={true}
  />
);
