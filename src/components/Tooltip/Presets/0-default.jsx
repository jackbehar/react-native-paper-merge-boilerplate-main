import React from 'react';
import Tooltip from '../Tooltip';
import IconButton from '../../IconButton/IconButton';

export default (
  <Tooltip uxpId='tooltip-1' title="I'm a Tooltip">
    <IconButton icon='camera' size={20} uxpId='tooltip-icon-button-1' />
  </Tooltip>
);
